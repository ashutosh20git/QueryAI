---
title: ACP
description: Używanie QueryAI z protokołem Agent Client Protocol (ACP).
---

import { Tabs, TabItem } from "@astrojs/starlight/components"

QueryAI obsługuje [Agent Client Protocol](https://agentclientprotocol.com) (ACP), który umożliwia korzystanie z QueryAI bezpośrednio w edytorach i środowiskach IDE.

:::tip
Listę edytorów i narzędzi obsługujących ACP znajdziesz w [raporcie postępu ACP](https://zed.dev/blog/acp-progress-report#available-now).
:::

ACP to otwarty standard, który ujednolica komunikację między edytorami kodu a agentami kodującymi AI.

---

## Konfiguracja

Aby używać QueryAI przez ACP, skonfiguruj swój edytor tak, aby uruchamiał polecenie `queryai acp`.

Uruchamia to QueryAI jako podproces zgodny z ACP, który komunikuje się z edytorem za pomocą JSON-RPC przez stdio.

Poniżej znajdują się przykłady dla edytorów obsługujących ACP.

---

### Zed

Zainstaluj QueryAI z [rejestru ACP Zed](https://zed.dev/docs/ai/external-agents#registry), uruchamiając `zed: acp registry` w palecie poleceń.

Aby zamiast tego użyć niestandardowego pliku wykonywalnego QueryAI, dodaj go do konfiguracji [Zed](https://zed.dev) (`~/.config/zed/settings.json`):

```json title="~/.config/zed/settings.json"
{
  "agent_servers": {
    "QueryAI": {
      "type": "custom",
      "command": "queryai",
      "args": ["acp"]
    }
  }
}
```

Aby użyć, wybierz `agent: new thread` w **Palecie poleceń** (Command Palette).

Możesz także przypisać skrót klawiszowy, edytując `keymap.json`:

```json title="keymap.json"
[
  {
    "bindings": {
      "cmd-alt-o": [
        "agent::NewExternalAgentThread",
        {
          "agent": {
            "custom": {
              "name": "QueryAI",
              "command": {
                "command": "queryai",
                "args": ["acp"]
              }
            }
          }
        }
      ]
    }
  }
]
```

---

### JetBrains IDE

Dodaj do swojego [JetBrains IDE](https://www.jetbrains.com/) plik `acp.json` zgodnie z [dokumentacją](https://www.jetbrains.com/help/ai-assistant/acp.html):

```json title="acp.json"
{
  "agent_servers": {
    "QueryAI": {
      "command": "/absolute/path/bin/queryai",
      "args": ["acp"]
    }
  }
}
```

Następnie wybierz „QueryAI” w selektorze agentów AI Chat.

---

### Avante.nvim

Dodaj do konfiguracji [Avante.nvim](https://github.com/yetone/avante.nvim):

```lua
{
  acp_providers = {
    ["queryai"] = {
      command = "queryai",
      args = { "acp" }
    }
  }
}
```

Jeśli potrzebujesz przekazać klucz API:

```lua {6-8}
{
  acp_providers = {
    ["queryai"] = {
      command = "queryai",
      args = { "acp" },
      env = {
        QUERYAI_API_KEY = os.getenv("QUERYAI_API_KEY")
      }
    }
  }
}
```

---

### CodeCompanion.nvim

Aby używać QueryAI jako agenta ACP w [CodeCompanion.nvim](https://github.com/olimorris/codecompanion.nvim), dodaj następującą konfigurację do Neovim:

```lua
require("codecompanion").setup({
  interactions = {
    chat = {
      adapter = {
        name = "queryai",
        model = "claude-sonnet-4",
      },
    },
  },
})
```

Ta konfiguracja sprawia, że CodeCompanion używa QueryAI jako agenta ACP do czatu.

Jeśli wymagany jest `QUERYAI_API_KEY`, zapoznaj się z sekcją [Konfigurowanie adapterów: Zmienne środowiskowe](https://codecompanion.olimorris.dev/getting-started#setting-an-api-key) w dokumentacji CodeCompanion.nvim, aby uzyskać szczegółowe informacje.

## Wsparcie

QueryAI działa przez ACP tak samo, jak w terminalu. Obsługiwane są wszystkie główne funkcje:

:::note
Niektóre polecenia z ukośnikiem, takie jak `/undo` i `/redo`, nie są obecnie obsługiwane przez ACP.
:::

- Narzędzia wbudowane (operacje na plikach, terminal itp.)
- Niestandardowe narzędzia i polecenia z ukośnikiem
- Serwery MCP zdefiniowane w konfiguracji QueryAI
- Zasady projektu z `AGENTS.md`
- Niestandardowe formatery i lintery
- Agenci systemowi i uprawnienia
