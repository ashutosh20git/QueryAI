sst-env.d.ts
packages/desktop/src/bindings.ts
packages/client/src/generated/
packages/client/src/generated-effect/

# Config fixtures are compared byte-for-byte by test/config/v2-compat.test.ts,
# so reformatting them would change what the tests assert.
packages/queryai/test/config/fixtures/

# Snapshot files are written by `bun test --update-snapshots`, not by hand.
**/__snapshots__/

# Checked in as symlinks (git mode 120000). Prettier would rewrite them into
# regular files on a checkout without symlink support, and on Linux it would
# format the same file twice, once through each path.
packages/app/public/site.webmanifest
packages/app/src/custom-elements.d.ts
packages/console/app/public/site.webmanifest
packages/docs/openapi.json
packages/enterprise/public/site.webmanifest
packages/enterprise/src/custom-elements.d.ts
packages/web/public/site.webmanifest

# Generated by release-pr.yml from commit subjects. Prettier would reflow them
# into something that no longer matches the commits, and the release pull
# request has to pass the `format` gate like any other.
CHANGELOG.md
